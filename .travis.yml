language: go

go:
  - 1.10.x

env:
  - DEP_VERSION="0.5.3"

before_install:
  - curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh

install:
  - dep ensure

before_script:
  - go vet ./...

script:
  - make test

notifications:
  email: false
